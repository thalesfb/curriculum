# Build outputs & generated files
node_modules/
dist/
# Astro build cache (regenerated on every build)
.astro/
# Generated CVs in markdown (4 roles × 3 langs = 12 files)
output/

# Environment & secrets
.env
.env.*
!.env.example
# Local Claude IDE settings (developer-specific, not project-wide)
.claude/

# Local/temporary
*.log
.DS_Store
Thumbs.db
/tmp/

# IDE & dev tools (local to each developer)
.vscode/
.idea/

# Root refs/ only (not public/refs/ which has downloadable PDFs)
/refs/

# Internal context files (not for public repo)
CONTEXT.md
QUICKSTART.md